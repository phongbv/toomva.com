# Database Configuration
# Development: SQLite (local file-based database)
DATABASE_URL="file:./dev.db"

# Production: Cloudflare D1 (uncomment and use when deploying)
# DATABASE_URL will be automatically set by Cloudflare Workers binding in production
# No need to manually set DATABASE_URL for D1 in production environment

# Next.js Configuration
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Optional: Dictionary API URL (default is already set in code)
# DICTIONARY_API_URL=https://api.dictionaryapi.dev/api/v2/entries/en
